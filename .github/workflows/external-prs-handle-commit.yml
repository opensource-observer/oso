# This is a test meltano pipeline
name: external-prs-handle-commit
env:
  X_GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  TAP_POSTGRES_AIRBYTE_CONFIG_PASSWORD: ${{ secrets.DB_PASSWORD }}
  TAP_POSTGRES_AIRBYTE_CONFIG_HOST: ${{ secrets.DB_HOST }}
  TAP_POSTGRES_AIRBYTE_CONFIG_DATABASE: ${{ vars.DB_NAME }}
  TAP_POSTGRES_AIRBYTE_CONFIG_PORT: ${{ vars.DB_PORT }}
  TAP_POSTGRES_AIRBYTE_CONFIG_USERNAME: ${{ vars.DB_USERNAME }}

on:
  pull_request_target:

jobs:
  external-prs-handle-commit:
    name: external-prs-handle-commit
    environment: testing
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
          run_install: |
            - recursive: true
              args: [--frozen-lockfile, --strict-peer-dependencies]

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20.x"
          cache: "pnpm"

      # If this was made by an owner then automatically trigger the test deployment
      - name: Run pnpm 