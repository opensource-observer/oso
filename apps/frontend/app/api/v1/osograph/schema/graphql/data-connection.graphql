enum DataConnectionType {
  POSTGRESQL
  GSHEETS
  BIGQUERY
}

type DataConnection {
  id: ID!
  orgId: ID!
  name: String!
  type: DataConnectionType!
  createdAt: DateTime!
  updatedAt: DateTime!

  # TODO: Add fields for the data connector configuration
}
