kind: Subgraph
version: v1
definition:
  generator:
    rootPath: .
  envFile: .env.globals.cloud
  includePaths:
    - auth-config.cloud.hml
    - compatibility-config.hml
    - graphql-config.hml
  name: globals
