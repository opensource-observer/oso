kind: Subgraph
version: v1
definition:
  generator:
    rootPath: .
  envFile: .env.globals.local
  includePaths:
    - auth-config.local.hml
    - compatibility-config.hml
    - graphql-config.hml
  name: globals
